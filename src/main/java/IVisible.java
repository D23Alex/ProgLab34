/**
 * Интерфейс видимого объекта. Имплементирующие его объекты должны давать
 * некоторое описание себя - что видит существо, которое на этот объект смотрит
 * @author Алексей
 *
 */
public interface IVisible {
	// от этого пока отказываемся и придаём эмоции всем людям
}
